type Link {
  url: String!
  hash: String!
}

input LinkInput {
  url: String!
  hash: String!
}

type Query {
  lookupHash(hash: String): String @function(name: "lookupHash-${env}")
}

type Mutation {
  saveLink(input: LinkInput): Link @function(name: "saveLink-${env}")
}